# 文本触发提醒 TextBanner

一个常驻 Windows 系统托盘的「悬浮提示栏」工具。它不再关心“某个程序被打开”，而是**单纯根据文本触发**：
只要 **浏览器标签、文件夹、文本文件内容、或任意窗口标题** 里出现了你指定的文字，就会在屏幕角落弹出一条醒目的悬浮提示。

> 本项目取代了旧版 `AppLaunchNotify\AppLaunchNotify.ahk`（那个只能检测进程/窗口“从无到有”，配置写死在脚本里，且无法监听浏览器标签和文件内容）。

---

## 一、怎么运行

- **直接运行**：双击 `publish\TextBanner.exe`。首次运行会在托盘出现一个蓝色图标，并自动打开设置窗口。
- 程序没有主窗口概念：关闭设置窗口后它仍在后台运行，从**任务栏托盘图标**退出。

### 从源码构建（可选）

```powershell
cd TextBanner
dotnet publish -c Release -o publish
```

需要 .NET 8 SDK。产物为依赖框架的版本（本机已有 .NET 8 运行时即可运行）。

---

## 二、四种触发来源

每条规则都有：**触发来源**、**匹配文本**、**来源限定**、**显示文本**、**显示时长**、**显示大小**、**提示条颜色**、**触发后打开（可选）**。

| 来源 | 监听什么 | 匹配文本对谁生效 |
|------|----------|------------------|
| 浏览器标签 | 扩展/CDP 检测**新打开的标签页**（含后台标签） | 标签标题 / 标签地址 URL |
| 窗口名字 | 所有可见顶层窗口的标题 | 窗口标题 |
| 文件夹 | 已打开的资源管理器窗口的**完整路径** | 文件夹路径 |
| 文本文件 | 指定文件 / 文件夹 / 通配符下的文本文件内容 | 文件内容 |

**匹配规则**：不区分大小写；多个词用 `|` 或 `;` 分隔表示“任一命中”。窗口/文件/文件夹来源从“未命中”变为“命中”的瞬间触发一次；**浏览器标签来源只在“新打开标签页”时触发一次**（有冷却时间防抖）。

**触发后打开**：触发时除了弹提示，可同时打开一个或多个网址 / 文件夹 / 文件（如 `https://…`、`D:\logs`、`D:\a.txt`，多个用 `;` 分隔），留空则不打开。

### 你给的例子，默认已经配好

- 触发来源：`浏览器标签`
- 匹配文本：`DSH`
- 显示文本：`远程触发任务目前已经配置：mybot；odysseus可能被分配到部分其它任务。`

于是当你**新打开**一个标题里带 `DSH` 的浏览器标签时，右上角就会弹出这条提示（标签增删、切换、刷新都不会重复触发）。

### 来源限定

- 浏览器标签：填进程名（如 `chrome;msedge`），留空 = 所有浏览器。
- 窗口名字：填“标题还需包含的文字”（如 `记事本`），留空 = 任意窗口。
- 文件夹：填要监听的**文件夹路径**（如 `D:\某个文件夹` 或 `文件夹名`），在该文件夹被打开时触发一次。
- 文本文件：填 **文件路径 / 文件夹 / 通配符**，例如：
  - `D:\logs\result.txt`（单个文件）
  - `D:\logs`（监视该文件夹里常见文本文件：txt/log/md/json/csv/ini/yaml/xml/cfg 等）
  - `D:\logs\*.log`（通配符）

文件内容按 UTF-8 / GBK 自动识别（带 BOM 优先），中文日志也能匹配。

---

## 三、悬浮提示栏

- 置顶、圆角、带阴影，多条提示会按位置自动堆叠，到达时长后淡出消失。
- **双击**提示栏 → 打开设置界面。
- **单击 ✕** 或 **右键**提示栏 → 立即关闭本条。
- **长按左键约 0.6 秒** → 固定/取消固定本条（固定后不再自动消失，边框会变成该规则的颜色；右键或 ✕ 仍可关闭）。
- 按住提示栏**拖动**（松手后停在原地，下次弹新提示会回到设定位置）。
- 显示文本支持 **Markdown**：`**加粗**`、`*斜体*`、`` `代码` ``、`~~删除线~~`、`[链接](url)`、`# 标题`、`- 列表`，样式可**叠加/嵌套**（如 `**加粗*斜体***`）；编辑器上方有**格式工具栏**一键插入。
- 每条规则可自定义**提示条颜色**（左侧色条与标题颜色），也可选 **跟随主题**，让强调色跟随当前主题。
- 点击某条规则的 **▶ 测试这条提示**，或托盘菜单的 **测试提示**，可即时预览效果。

---

## 四、设置界面

- **规则**：左侧列表增删/复制规则，右侧编辑触发条件、显示文本（Markdown，带格式工具栏）、触发后打开（可选，支持多条）、时长（1–120 秒）、大小（小/中/大）、提示条颜色。改动自动保存。
- **事件记录**：最近 500 条触发记录（时间、规则、来源、命中的原文），便于排查“为什么没触发 / 为什么误触发”。
- **常规设置 → 外观**：**主题**（跟随系统 / 明亮 / 夜间 / 薄荷 / 暮紫 / 绯红，三色调简约风，切换即时生效并作用于设置界面与提示条；「跟随系统」会随 Windows 亮/暗自动切换）、**语言**（中文 / English，切换即时生效）。
- **常规设置**：提示栏位置（四角）、默认时长、检测间隔、触发冷却、浏览器进程名、CDP 调试端口（含扩展/CDP 实时连接状态）、开机自启、暂停监控。

---

## 五、托盘菜单

| 菜单 | 作用 |
|------|------|
| 打开设置 | 打开设置窗口 |
| 规则列表 | 直接列出所有规则（● 启用 / ○ 停用），点击跳转到该规则 |
| 事件记录 | 打开设置并切到“事件记录”页 |
| 暂停 / 恢复监控 | 一键停掉所有监听 |
| 测试提示 | 弹一条测试横幅 |
| 退出 | 完全退出程序 |

双击托盘图标 = 打开设置。

---

## 六、配置文件与开机自启

- 配置文件：`%APPDATA%\TextBanner\config.json`（UTF-8，直接可读可改，改完在托盘“退出”后重开生效）。
- 异常日志：`%APPDATA%\TextBanner\error.log`（正常运行时不会生成）。
- 开机自启：设置 → 常规设置 → 勾选“开机自动启动”，写入注册表 `HKCU\...\Run`，登录后最小化到托盘（等价于 `--minimized`）。
- `--test` 启动参数：启动即弹一条测试横幅，用于预览样式。

---

## 七、浏览器标签检测（扩展推荐 + CDP 备用）

「浏览器标签」来源只在**新打开标签页**时触发一次。两种接入方式二选一（都接也可以，扩展优先）：

### 方式 A：浏览器扩展（推荐，最省心）

无需加启动参数、无需重启浏览器，一次安装即可：

1. Chrome：地址栏打开 `chrome://extensions` → 开启右上角「开发者模式」→「加载已解压的扩展程序」→ 选择本项目的 `browser-extension` 文件夹。
2. Edge：地址栏打开 `edge://extensions` → 开启「开发人员模式」→「加载解压缩的扩展」→ 选择**同一个**文件夹。

装好后，扩展会把所有标签（含后台标签）实时推送给本程序（`127.0.0.1:51739`）。设置界面「常规设置 → CDP 调试端口」下方会显示「扩展已连接：chrome / msedge」。

### 方式 B：CDP 调试端口（备用）

1. 给浏览器快捷方式加启动参数：Chrome ` --remote-debugging-port=9222`，Edge ` --remote-debugging-port=9223`。
2. **务必先完全退出浏览器**（含后台进程）再用改好的快捷方式启动，否则端口不生效。
3. 确认「CDP 调试端口」为 `9222;9223`（默认）。

> 两种方式都基于“标签 ID 差量对比”，只在**新标签**出现且命中时触发一次；标签增删、切换、刷新都不会重复触发。若想按“当前活动标签标题”触发，请改用「窗口名字」来源并填浏览器进程名（如 `chrome;msedge`）。
